{
  "name": "bitvibe-extension",
  "version": "0.1.0",
  "private": true,
  "description": "Build tooling for the MakeCode AI Chrome extension",
  "scripts": {
    "build": "node scripts/build.mjs",
    "build:watch": "node --watch scripts/build.mjs",
    "package": "npm run build && node scripts/package.mjs"
  }
}
